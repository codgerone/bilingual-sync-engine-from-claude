# Bilingual Sync Engine - Python Dependencies

# LLM Providers
anthropic>=0.18.0          # Anthropic Claude API
openai>=1.0.0              # OpenAI-compatible APIs (DeepSeek, Qwen, Doubao, Zhipu)

# XML Processing
defusedxml>=0.7.1          # Safe XML parsing

# Chinese Word Segmentation (for word-level diff)
jieba>=0.42.1

# Optional: HTTP requests (for Wenxin API)
requests>=2.28.0

# Optional: Progress bars
tqdm>=4.65.0

# Optional: Logging
loguru>=0.7.0

# Notes:
# 1. pandoc - install separately for document verification
# 2. Document Library - from docx skill, no pip install needed
